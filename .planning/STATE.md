# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-09-14)

**Core value:** Drafts sound like the executive and follow a proven format, so at least 80% get approved with only light edits and a week of content costs them about an hour instead of eight.
**Current focus:** **Phase 5 (Zernio Push) is three plans in, and the project has finally touched Zernio.** 05-01 built `src/zernio.ts`, the publishing boundary; 05-02 built the storage half (four nullable push columns and two run-scoped writes, on local and remote D1); 05-03 built `/zernio` and **loading it made the first real Zernio request ever** — 200, one connected LinkedIn account, which closes the roadmap's "is the API available on Vincent's plan" question empirically. Phase 4 remains code-complete, deployed, and **unobserved on production**; draft quality is still unaddressed and the two open todos are what move the number

## Current Position

Phase: 5 of 5 (Zernio Push) — in progress
Plan: 05-01 ✓ 05-02 ✓ 05-03 ✓ of 4. Waves: [05-01 ✓ + 05-02 ✓, in parallel, zero file overlap] → [05-03 ✓] → [05-04]. Phase 4 complete: 04-01 ✓ 04-02 ✓ 04-03 ✓. Phase 3 complete: 03-01 ✓ … 03-07 ✓
Status (Phase 5): `npm run check` 0, `npm test` 68/68 (05-03 adds none — it is routes and rendering). **The Zernio API works on this plan, and that is now observed rather than assumed:** `/zernio` called `GET /v1/accounts?platform=linkedin&status=connected` against the real service and got **200 with one connected LinkedIn account** (`Vincent Sider`, id `YOUR_ZERNIO_ACCOUNT_ID`, `isActive: true`). Both 05-03 `user_setup` items were already in place, so the plan ran fully autonomous with no checkpoint. `ZERNIO_USER_TOKEN` in `.dev.vars` is a **valid** Zernio API key; whether the **Worker secret** holds that same value is still unknown (`/health` reports presence only). 05-02 landed `migrations/0006_push.sql` on **local and remote** D1 and the two push writes in `src/db.ts` (`recordPush` clears the error columns, `recordPushFailure` keeps the post id, both run-scoped, both observed changing 0 rows cross-run). `src/db.ts` still has no import lines at all. **`POST /v1/posts` has still never been called** — the write path is implemented-from-spec and unobserved. **Nothing has been deployed** — 05-04 owns it, so production has neither `/zernio` nor the push route while remote D1 already carries 0006
Status (Phase 4, unchanged): Deployed as version `734bc0e6-7963-4d70-8db8-349b27102d07`, so remote code and migration 0005 are back in step. The drafting prompt now carries an `<approved_posts>` block between the voice samples and the transcript — 3 posts, 2500 chars each, rendered even when empty, excluded from `checkGrounding`'s source pool on purpose. `npm run check` 0, `npm test` 61/61, `src/prompts.ts` still zero imports. **The 04-03 checkpoint was approved with no observations reported** (the second in a row), so `/health`, the decision round trip on production D1, the grounding panel against a real draft and DRAFT-04 end to end are all **approved-but-unobserved, not verified**. **The 80% approval target remains unmet and untested since run 1**
Last activity: 2026-09-15 — Completed 05-03-PLAN.md (`/zernio`: the connection page and the account choice). Two commits, one new file and three touched; four page states plus three extra paths driven and observed, three of them against a local stand-in with `src/zernio.ts` restored SHA-identical. The state that mattered was driven against the **real API**. Earlier: completed 05-02-PLAN.md (migration 0006 and the push writes), which ran in parallel with 05-01-PLAN.md (the Zernio client and its publish-safety pin); wave 1 of Phase 5 is complete with zero file overlap between the two. Earlier the same day: completed 04-03-PLAN.md (the feedback loop and the phase deploy), then verified the phase. 04-VERIFICATION.md status is `human_needed`, not `passed`: **16/17 must-haves verified against the codebase, 0 failed**, with the single open item being live observation rather than missing code. The verifier independently re-proved the load-bearing parts rather than trusting the SUMMARYs — migration 0005 present on **both** databases (`wrangler d1 migrations list --remote` reaches D1 without passing the Access-gated hostname), `body` touched by only two UPDATEs in the whole codebase, the CRLF normalisation that keeps accept from registering as edit, run-scoped `WHERE id = ?4 AND run_id = ?5`, and approved posts genuinely absent from `checkGrounding`'s pool. 9/9 key links wired, 5/5 artifacts substantive, zero placeholder patterns. Completed 04-03-PLAN.md (the feedback loop and the phase deploy). Verified locally by capturing the real OpenAI request body against a stand-in endpoint: block order `voice_samples` → `approved_posts` → `transcript` → `format`, another run's approved post present, the run's own accepted draft absent from the whole payload, no meeting title or speaker label anywhere

Progress: █████████▌ 95% (19 of 20 plans complete; Phase 5 is planned as 05-01 … 05-04)

## Performance Metrics

**Velocity:**
- Total plans completed: 19
- Average duration: ~8 min agent time
- Total execution time: ~2.3 hours agent time (excluding user time on the Cloudflare dashboard, filling .dev.vars, and the 02-03 / 03-04 / 03-07 / 04-03 verification checkpoints)

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 1 | 3/3 | ~33 min | ~11 min |
| 2 | 3/3 | ~15 min agent (~27 min wall) | ~5 min |
| 3 | 7/7 | ~61 min agent (~3h wall) | ~9 min |
| 3 (gap closure) | 3/3 | ~33 min agent | ~11 min |
| 4 | 3/3 | ~26 min agent | ~9 min |
| 5 | 3/4 | ~24 min agent (~20 min wall; 05-01 and 05-02 ran in parallel) | ~8 min |

**Recent Trend:**
- Last 5 plans: 01-02 (~15 min, includes a human-action pause), 01-03 (~15 min agent, ~42 min wall with two human pauses), 02-01 (~3 min, fully autonomous TDD), 02-02 (~4 min, fully autonomous, ran in parallel with 02-01), 02-03 (~8 min agent, ~20 min wall with one human-verify checkpoint)
- Wave 1 of Phase 3 ran two plans in parallel with zero file overlap (03-01: prompts/tests; 03-02: migration/db/openai). 03-02 took ~8 min fully autonomous
- 03-03 (~5 min, fully autonomous): the fastest plan yet, because 03-02 had already shaped the reads the page needed — the only work was rendering and validation
- 03-04 (~19 min agent, ~2h wall): the longest agent time of the phase, because the whole paid path was verified for free first — nine page states and every failure mode driven against a deliberately invalid key before a penny was spent
- 03-06 (~9 min, fully autonomous, gap closure wave): a migration, two files and a rendering change; about half the time went on seeding local D1 with a long transcript, a short one and a pre-migration run so all three coverage states could be seen rather than reasoned about
- 03-05 (~12 min, fully autonomous TDD, ran in parallel with 03-06): RED/GREEN took ~5 min; the other ~7 went on calibrating against the real production drafts — exporting run 1, sweeping thresholds, and checking whether a flagged sentence was actually a fabrication before accepting the verdict. The measurement is what made the SUMMARY honest, and it contradicted the plan
- 03-07 (~12 min agent, ~25 min wall with one blocking checkpoint): two code commits and a deploy; roughly half the agent time went on seeding local D1 with seven drafts so all six render branches — including a deliberately malformed `grounding_json` — were seen rather than reasoned about
- 04-01 (~7 min, fully autonomous): a migration and one file. Roughly half the time went on seeding local D1 with two runs so the run-scoping guard could be *measured* (`SELECT changes()` after each UPDATE) rather than reasoned about — the cross-run UPDATE changing 0 rows is the whole security claim of the plan
- 04-03 (~14 min agent, two sessions either side of one blocking checkpoint): three files, four new tests and the phase deploy. Roughly half the agent time went on verification the plan did not ask for — capturing the real OpenAI request body against a local stand-in endpoint, because "the read ran" and "the right bytes went out in the right order with the right things missing" are different claims, and DRAFT-04 depends on the second. The checkpoint returned no observations, so the plan's most important output is a list of what is still unobserved
- 04-02 (~5 min, fully autonomous): the fastest plan since 03-03, and for the same reason — 04-01 had already shaped every read and write the page needed, so the work was rendering, one route and validation. One file touched. The verification (eight plan checks plus eight rejection paths, all driven through the live route with curl) cost more than the code
- 05-01 (~4 min, fully autonomous, ran in parallel with 05-02): the fastest plan of the project. Two new files, no existing file touched, no D1 and no network. The only work beyond writing the client was the mutation pass — breaking the safety property three ways and restoring the file SHA-identical each time, because a test asserting a field is absent passes trivially against a file that never had it
- 05-02 (~4 min, fully autonomous, ran in parallel with 05-01): tied with 05-01 as the fastest plan of the project, and the reason here is structural — it is the third time this codebase has added nullable columns to an existing table and a run-scoped UPDATE beside them (0004, 0005, now 0006), so the shape was decided before the plan was written. Most of the time went on the verification: compiling the module and capturing the helpers' real SQL through a stub D1 rather than retyping it
- 05-03 (~16 min, fully autonomous, no checkpoint): four times the length of the two wave-1 plans, and all of the extra went on observation rather than code. The code is one page; the verification was seven rendered states, three of them impossible to produce from the real API and therefore driven against a local stand-in server with `src/zernio.ts`'s `ENDPOINT` temporarily repointed and restored SHA-identical. The technique worth keeping: "no request was made" was **measured** by counting outbound `fetch` spans to `zernio.com` in wrangler's local observability API before and after (7 → 7), instead of arguing it from the source
- Trend: steady — Phase 2 averaged ~5 min per plan; the wall-clock cost is concentrated in the verification checkpoints (02-03, 03-04, 03-07), which is exactly where a human should be in the loop (first real executive data, then first real output, then the instrument that judges it)

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Requirements: API keys live in Worker secrets, not a settings screen (single user in v1)
- Requirements: delete-on-request met by a wrangler D1 command in v1; UI deletion deferred to VOICE-09/10
- Roadmap: DRAFT-04 (feedback loop) verified in Phase 4, where approvals first exist
- Phase 1 plan: Hono 4.13 + hono/jsx server rendering (no build step, typed D1 bindings); no static assets binding because it drops the Access context
- Phase 1 plan: Access gate = hostname-based Access app on the workers.dev URL + Worker checks `ctx.access` and returns 403 without it (fail closed); preview_urls disabled
- Phase 1 plan: D1 schema starts with only a `settings` table; each later phase adds its own migration
- Phase 1 plan: secrets are OPENAI_API_KEY, FIREFLIES_API_KEY, METRICOOL_USER_TOKEN, pushed with `wrangler secret bulk .dev.vars`; no test framework until Phase 2 (speaker filtering is the first TDD candidate)
- 01-01: tsconfig uses `lib: ["ES2022"]` (no DOM) + `skipLibCheck` so wrangler-generated types and hono/jsx types coexist; keep it that way
- 01-01: `<style>` blocks use `dangerouslySetInnerHTML` (hono/jsx escapes text children)
- 01-01: Worker is live at https://pipeflick.your-subdomain.workers.dev; D1 `pipeflick-db` id `YOUR_D1_DATABASE_ID`, region WEUR
- 01-02: Access app created in the Cloudflare dashboard (Workers & Pages → pipeflick → Settings → Access), not via `scripts/create-access-app.sh`; no Access-scoped API token exists. Zero Trust team `your-team` (auto-generated), team domain `your-team.cloudflareaccess.com`, app AUD `YOUR_ACCESS_AUD`
- 01-02: `ctx.access` works with the hostname-based app; jose JWT fallback not needed. Every route sits behind `app.use("*", requireAccess)`; read the user as `c.get("email")`. Local dev identity comes from `access.dev` in wrangler.jsonc
- 01-03: Zernio replaces Metricool as the scheduler; the secret is ZERNIO_USER_TOKEN (not METRICOOL_USER_TOKEN). Phase 5 must be re-researched and re-planned against the Zernio API before execution. ROADMAP/REQUIREMENTS/PROJECT were updated to Zernio at Phase 2 completion (2026-09-15); the phase directory is now `05-zernio-push`. The only remaining Metricool mention is PROJECT.md naming it as a competitor tool, which is correct
- 01-03: secret names live only in `src/env.ts` SECRET_NAMES; health page and bindings type derive from it. Adding a secret = SECRET_NAMES entry + `.dev.vars.example` line + `npm run secrets:push`
- 01-03: `/health` returns 500 when the D1 read-back fails so scripted checks catch a broken binding
- 02-01: test framework is plain vitest 5 (`npm test` = `vitest run`), tests in `test/**/*.test.ts` outside tsconfig `include: ["src"]` so `npm run check` is unaffected; no @cloudflare/vitest-plugin until a Workers-runtime test actually needs one
- 02-01: `src/speaker-filter.ts` is the only place that decides which transcript text may be stored — pure, no I/O, no logging. Exports `keepSpeakerLines`, `countBySpeaker`, `matchSpeaker`, `normaliseLabel`, `UNKNOWN_SPEAKER`, types `Sentence`/`SpeakerCount`. Every import must run through it before any D1 write
- 02-01: speaker matching is always on the normalised label (trim, collapse whitespace, lowercase); `matchSpeaker` returns null when blank, absent or ambiguous, and a blank label to `keepSpeakerLines` keeps nothing (fail closed, never dump a whole meeting)
- 02-02: migration 0002 adds `transcripts` (PK = the Fireflies transcript id, so re-import upserts) and `voice_samples`; applied to local AND remote D1. The compliance constraint (only the executive's lines, no participant emails/summaries/URLs) is written into the migration header, not just policy docs
- 02-02: D1 helpers live in `src/db.ts` on the bound-parameter rule — `upsertTranscript`, `listTranscripts` (returns `TranscriptSummary`, no body column), `getTranscript`, `insertVoiceSample`, `listVoiceSamples`. Speaker name is stored in `settings` under `SPEAKER_NAME_KEY` = `speaker.name`
- 02-02: `csrf()` from hono/csrf is registered app-wide right after `requireAccess`, so no future POST route can forget the Origin check. Form pattern is POST → validate → 303 redirect with a `?saved=1` flash; no client JS anywhere
- 02-02: feature routers are `export const x = new Hono<AppEnv>()` mounted with `app.route("/", x)`; `src/layout.tsx` owns the shared nav (Home, Sources, Fireflies, Settings, Health) and all CSS
- 02-02: an empty speaker name is valid and clears the setting; route-level validation (id `^[A-Za-z0-9_-]{1,100}$`, sample ≤ 20000 chars, name ≤ 200 chars) returns plain-text 400 before touching D1
- 02-03: `src/fireflies.ts` takes the API key as an argument and never reads env, never logs, and never puts the key, request body or transcript text in an error. Routes are the only place touching `c.env.FIREFLIES_API_KEY`; a missing key renders a 500 pointing at /health and makes no request
- 02-03: Fireflies returns HTTP 200 with a populated `errors[]` array, so both status and body are checked; `FirefliesError` carries `code` (Fireflies code, else `http_<status>`). `object_not_found` → 404, everything else → 502 rendering Fireflies' own message plus code (never a stack)
- 02-03: one GraphQL request per page view — `user` and `transcripts(limit: 50, skip)` ride in the same query. An import costs 2 requests (preview + confirm) because the confirm re-fetches rather than posting sentences from the browser, keeping other participants' text inside the Worker
- 02-03: a speaker option is selectable only when `keepSpeakerLines` actually returns lines for that label, which disables the `UNKNOWN_SPEAKER` group; `matchSpeaker` is given only importable labels; zero kept lines is a 422 re-render that writes nothing
- 02-03: Fireflies plan tier is unconfirmed — the Free-plan budget of 50 requests/day stays the working assumption; any future polling/webhook feature must re-check the tier first
- 03 plan: architecture is one OpenAI call per Worker invocation driven by D1 job rows (migration 0003: `runs`, `outliers`, `drafts`) plus a self-submitting POST form. Rejected: all-calls-in-one-request (no status, loses everything on disconnect), `Promise.all` (no status, no prompt cache), `ctx.waitUntil` (caps at 30s), Queues / Durable Objects / Workflows (all free-tier available, but over-build for one user), cron sweep (60s granularity). Revisit Workflows if a run grows past ~10 steps
- 03 plan: models are `gpt-5.6-terra` (extraction) and `gpt-5.6-sol` (drafting), both `reasoning.effort: "low"`, via the **Responses** API (`text.format` json_schema — flattened, NOT Chat Completions' `response_format`). Raw `fetch`, no `openai` package. ~$0.19/run. The quality lever if approval is under 80% is one constant: swap drafting to `gpt-6-astra`
- 03 plan: `src/prompts.ts` is pure and import-free by design — `buildDraftingInput` takes primitives only, so no code path can send a Fireflies meeting title (which names counterparties) or a speaker label to OpenAI. Asserted in `test/prompts.test.ts`
- 03 plan: `store: false` on every OpenAI request (the Responses default is 30-day application-state retention). Abuse-monitoring logs are still kept 30 days; changing that needs a ZDR agreement — state it honestly, do not claim zero retention
- 03 plan: claim-then-work before every call (`SELECT` then conditional `UPDATE`, `meta.changes === 1`); no `RETURNING` (undocumented in D1). Auto-advance renders its `<script>` only when work is claimable, the last failure was retryable and `attempts < 2` — otherwise a bad key loops forever
- 03 plan: `outliers.template_json` is stored but never rendered, and is excluded from the `getRunView` projection so OUTL-02 is structural rather than a rendering discipline
- 03-01: `src/prompts.ts` is the phase's compliance boundary — zero imports, primitives-only builders. `buildDraftingInput(samples: string[], transcriptBody: string, template: Template)` must never gain a row-taking overload, and no call site may pass a meeting title "for context". Both halves are asserted in `test/prompts.test.ts` (payload excludes the title/speaker, and the source file contains no `import`)
- 03-01: every model id, token cap, timeout and character cap lives as a named constant in `src/prompts.ts`, not inline at the call site. The approval-rate lever is one line there: `MODEL_DRAFT` from `gpt-5.6-sol` to `gpt-6-astra`
- 03-01: `excerptTranscript` returns `{ text, linesUsed, linesTotal }` (counts, not a truncated flag) so the run page renders "using the first N of M lines" without a second pass over the body; a line longer than the whole 12000-char budget is truncated inside rather than dropped
- 03-01: `isGrounded` rejects whitespace-only citations as well as an empty array — `body.includes("")` is always true, so a naive check would score an empty citation as grounded. A false is a warning beside the draft, never a hard failure
- 03-01: the load-bearing instruction phrases (banned words, "Invent nothing.", "Do not name clients…", the never-mention-a-transcript rule, the em-dash ban) are pinned by test, so a reword cannot silently drop one
- 03-02: the claim UPDATE guards on `started_at` as well as `status` (`WHERE id = ?2 AND status = ?3 AND started_at IS ?4`, bound to the values just read). Status alone is not exclusive for a stale `running` row — two invocations would both match and both pay. `meta.changes === 1` still decides the winner; no `RETURNING`. Stale window `STALE_JOB_MS = 180_000`
- 03-02: `resetRunJobs` resets only `status != 'done'` rows (verified by before/after dump), so Retry never re-pays for a finished OpenAI call. `getRunView` omits both `template_json` and `outliers.body` at the query
- 03-02: `claimNextJob` returns the template as the raw JSON string, so `src/db.ts` imports nothing from `src/prompts.ts`; `finishOutlier`/`finishDraft` take the OpenAI `usage` object structurally and flatten it to `{input_tokens, output_tokens, reasoning_tokens}` before writing `usage_json`
- 03-02: migration 0003 added `drafts.grounded INTEGER` (not in 03-RESEARCH.md's DDL) because `finishDraft` must store the source-line check; `usage_json` on both job tables makes the cost estimate a measurement after two runs
- 03-03: run progress is always `done_jobs` of `total_jobs`, never "of 6" — `createRun` accepts 2 or 3 outliers, so a run has 5 or 6 steps and a hardcoded 6 would permanently misreport a finished two-outlier run
- 03-03: the status page's outlier excerpt is cut by SQLite `substr(body, 1, 80)` inside the `getRunView` projection (`OUTLIER_EXCERPT_CHARS`), so the page stays at three reads and the rest of the pasted post never enters the Worker. `template_json` remains unselected — that rule is untouched
- 03-03: `getRunView` LEFT JOINs the transcript title into `RunView.transcript_title` (the `listRuns` pattern), so a deleted transcript renders "(transcript deleted)" instead of dropping the run
- 03-03: an ungrounded draft gets amber `.notice.warn`, not red `.notice.error` — it is a reviewer's cue, not a failed step, and the draft is always still shown
- 03-03: a malformed run id is 400 and an unknown run is 404, matching the `src/sources.tsx` transcript-detail precedent. `RUN_ID_PATTERN` guards the UUID shape before D1
- 03-03: route table is `GET /runs`, `GET /runs/new`, `POST /runs`, `GET /runs/:id`; components are `RunTable`, `NewRunForm`, `StepStatus`, `Failure`, `TemplateSteps`, `DraftSection`, `progress`. 03-04's auto-advance form belongs between the progress paragraph and `<h3>Templates</h3>`
- 03-02: `src/openai.ts` classifies by `error.code` — the four billing/spend/usage 429 codes are NOT retryable; `bad_json` and `bad_body` were added so no unhandled `SyntaxError` can 500 the step route and strand a claimed job. `retryable` is computed, NOT persisted: 03-04 resolved this by mapping `error_code` back in `RETRYABLE_ERROR_CODES` (no migration 0004 needed)
- 03-04: `RETRYABLE_ERROR_CODES` in `src/runs.tsx` is the code-to-retryability map, and it is **default-deny** — an unrecognised code halts the run. Being wrong that way costs one click; being wrong the other way burns the Free plan's daily request budget on a key that will never work
- 03-04: a failed step is **terminal until Retry**. `failJob` writes `failed` and `claimNextJob` only takes `pending`, so the `retryable && attempts < 2` guard decides whether the run keeps going **at all**, not whether the failed step is re-run. A transient timeout therefore costs one Retry click. Automatic single retry would need a `db.ts` helper to un-fail a row, because `resetRunJobs` zeroes `attempts`
- 03-04: auto-advance never renders while any job is `running`. Without it two open tabs loop forever (loser gets `claimNextJob` → null → 303 → re-render with the script intact). "Claimable" is defined as exactly what `claimNextJob` will take, never "not done" — a draft whose outlier failed is `pending` forever
- 03-04: `retryAfterSeconds` travels as a clamped `?retry_after=N` query parameter on the redirect, not a column. It is needed for one page render
- 03-04: `runs.status` is recomputed from the job rows after every step (`runStatus`/`reconcileRunStatus`), reusing the `getRunView` the drafting branch already needs. `createRun` writes `pending` once and nothing else maintained it
- 03-04: every `sk-` run is scrubbed out of an error message before it reaches D1 — OpenAI's `invalid_api_key` text quotes the key back masked, and CLAUDE.md forbids rendering any part of a secret
- 03-04: the Responses-API request shape from 03-02 (flattened `text.format`, `store: false`, `reasoning.effort: "low"`) is **confirmed against the live API**; `gpt-5.6-terra` and `gpt-5.6-sol` both resolve
- 03-06: migration 0004 adds `runs.transcript_lines_used`, `runs.transcript_lines_total` and `drafts.grounding_json` — all nullable, applied to local AND remote D1 (first attempt, no 7403 this time). `grounding_json` rides along empty so 03-07 needs no migration and remote D1 was altered once, not twice
- 03-06: coverage is **computed by the caller and stored as two primitives** — `createRun(db, transcriptId, bodies, { linesUsed, linesTotal })`. `src/db.ts` still imports nothing from `src/prompts.ts`; that boundary is the Jersey/JFSC guard and a convenience overload taking a transcript row would break it
- 03-06: the counts are frozen at run creation, never recomputed at render. A re-imported transcript changes the body, and a recomputed figure would drift away from the drafts sitting beside it
- 03-06: either count NULL renders **nothing** — not "unknown", not a guess from `line_count`. Runs predating 0004 have no coverage figure, and inventing one is the exact over-claiming the gap exists to fix
- 03-06: `listTranscripts` selects `length(body) AS body_chars` and `TranscriptSummary` is now `Omit<TranscriptRow, "body"> & { body_chars: number }`. The body never leaves D1; only its length does. It is a threshold for the new-run warning, never a displayed figure — the pre-run warning carries no number at all, because characters on one page against lines on another destroys trust in both
- 03-06: a cut transcript is amber `.notice.warn`, matching 03-03's ungrounded-draft treatment — the run is fine, the drafts simply had less to work with
- 03-04 (measured, run 1): **$0.097 per run**, about half the research's ~$0.19 estimate. 16,730 input / 1,979 output tokens across 6 calls; **zero** reasoning tokens on all three terra extractions. At two runs a week that is ~$0.80/month — model cost is not a constraint, quality is. The `MODEL_DRAFT` → `gpt-6-astra` lever is nearly free to test

- 03-05: `checkGrounding(post, sourceLines, sources)` replaces the citation-only proxy and is the DRAFT-03 guard from here on. Three passes: normalised citation resolution, per-sentence attribution (normalised containment, else ≥50% overlap of 4-token shingles), and a 6-token n-gram repeat detector over the whole post. `sources` is `[transcriptBody, ...voiceSampleBodies]` and **outlier bodies must never be passed in** — FORMAT carries shape, so an outlier's phrasing in a draft is a defect to catch, not grounding to credit
- 03-05: the repeat detector is a **token** n-gram stream, not sentence comparison, and that is load-bearing. The production failure was three sentences of 3, 2 and 2 tokens; only a stream that crosses sentence boundaries catches the repeated 7-token run
- 03-05: `GroundingReport` reports `skipped` as a first-class field. A short invented sentence said once is below both the claim and the repeat thresholds and is **not caught** — the count is the honest measure of the blind spot, and it is pinned by test rather than described in prose
- 03-05: the check matches **words, not meaning**. A model compressing a real transcript idea into its own vocabulary reads as unsupported, which is most of what `unsupported` contained on run 1. It is a list a reviewer reads, never a count they act on blindly
- 03-05: thresholds are `GROUNDING_MIN_CLAIM_TOKENS = 6`, `GROUNDING_SHINGLE_TOKENS = 4`, `GROUNDING_SUPPORT_RATIO = 0.5`, `GROUNDING_REPEAT_TOKENS = 6`. **None moved during calibration, and that is a measurement**: sweeping the ratio 0.5 → 0.25 changed 1-3 of 45 sentences and changed no draft's verdict. Move one only when a run exists where it changes an answer, and say which run in the source comment
- 03-05: `normaliseForMatch` strips outer punctuation, then up to **two** stacked leading connectives, then outer punctuation again. Order matters — a quote wrapped in quotation marks still needs its "And that" found, and "Right," must not leave its comma behind
- 03-05: `isGrounded` is left intact and marked superseded so `src/runs.tsx` keeps compiling; 03-07 owns the call site and the deletion. `src/prompts.ts` still has **zero imports**

- 03-07: **`isGrounded` is deleted.** The step route calls `checkGrounding(value.post, value.source_lines, [transcript.body, ...sampleBodies])` — the whole post, the full transcript body (a superset of the excerpt, so only ever more lenient), the voice samples already fetched for `buildDraftingInput`, and **no outlier bodies**. Still primitives only at the call site
- 03-07: **two columns, two audiences.** `grounded` stays a plain 1/0 because Phase 4's light-edit rate (APPR-05) counts it in SQL; `grounding_json` carries the counts and named lines a person reads. `finishDraft` takes a `GroundingRecord` typed **structurally in `src/db.ts`**, so that file still imports nothing from `src/prompts.ts` — the import would be the shorter line and the wrong one
- 03-07: the stored report is capped like error text — `MAX_GROUNDING_LINES = 5` entries of each list, `MAX_GROUNDING_LINE_CHARS = 300` each, with pre-cap totals kept so the page says "and N more"
- 03-07: **a draft with no stored report shows no verdict, and the legacy `grounded` boolean is never a fallback.** That boolean is the retired proxy; re-rendering it would keep a known-false signal alive on exactly the drafts carrying it wrong (run 1's draft 2 is stored `grounded = 1` with an invented slogan printed twice)
- 03-07: `grounding_json` is parsed defensively (`parseGrounding` re-derives every field from `unknown`); unreadable JSON renders the same "no report" hint and **cannot take the run page down**. Same discipline for any future D1 text column
- 03-07: the panel renders **below** the post, not above it — it can be five quoted lines plus three hints, and the draft must not be pushed off-screen. `supported / checked` renders in **every** state, not only the clean one, per 03-05's carry-forward. Unresolved citations get their own amber line so a `grounded = 0` draft can never render as clean
- 03-07: every state with a report also prints what was **not** examined ("N short or connecting lines were too generic to check"). The check matches words, not meaning, and Phase 3 already failed once by letting a narrow check read as a whole-output guarantee
- 03-07: `usage_json` now records `input_tokens_details.cached_tokens` (and `src/openai.ts`'s `Usage` declares it). Rows written before this commit have **no** `cached_tokens` key — absence means unknown, not zero cache hits

- 04-01: **migration 0005** adds `drafts.decision` ('accepted'|'edited'|'rejected'), `drafts.final_body` and `drafts.decided_at`, all nullable, applied to local **and remote** D1. NULL decision = undecided and is a real state. **`body` is never overwritten by an edit** — it is the model's original output and the load-bearing half of APPR-05; a row that lost it could never again say how much the executive changed
- 04-01: `final_body` is written on **accept** as well as edit (a copy of `body`), so "the final text" is a column and not a `COALESCE` every reader must remember. NULL only on rejected and undecided. The APPR-05 query lives in the migration header and has been run; `avg_char_delta` is documented as a **proxy** for edit size, never an edit distance, and is NULL (not 0) for rejected rows
- 04-01: **ownership is the WHERE clause.** `recordDecision(db, runId, draftId, decision, finalBody)` is one UPDATE scoped by `id` AND `run_id`, returning `meta.changes === 1`; the caller turns false into a 404. No separate ownership SELECT — that would be a second round trip and a race. Measured: the same draft id under the wrong run changes 0 rows. Deciding twice overwrites, and `decided_at` means the last decision
- 04-01: `listApprovedPosts(db, limit, excludeRunId)` returns `final_body` for `decision IN ('accepted','edited')`, newest first. **`excludeRunId` is not padding**: it keeps a run's own drafts out of its own prompt (stopping draft 3 echoing draft 1 after a mid-run approval) and keeps the cacheable prompt prefix byte-stable across a run's three drafting calls. `limit` stays a caller-computed primitive so `src/db.ts` still imports **nothing** from `src/prompts.ts`
- 04-01: `listRuns` carries per-draft decisions via **one extra flat query grouped in JS**, not four more correlated subqueries per row and not a per-run read. Two queries for the whole page, still no bodies. `DraftView` and `getRunView` carry `decision`, `final_body` and `decided_at`
- 04-01: the DB layer **does not derive 'edited'** — 04-02's route compares the submitted text against `body` and decides. A self-reported edit flag would measure what people claim rather than what they did

- 04-02: **two buttons, three outcomes.** Accept and Reject are the only controls; `POST /runs/:id/drafts/:draftId/decision` reads the draft's stored `body`, compares, and writes `accepted` or `edited` itself. There must never be an "Accept with edits" button — the derivation is what keeps APPR-05 a measurement instead of a survey
- 04-02: **`normalisePost` (CRLF→LF, then trim) runs on both sides of that comparison and on the stored value**, and it is load-bearing. Browsers submit textareas CRLF-normalised per the HTML spec and D1 holds LF, so without it every accept records as `edited` and the light-edit rate reads 0% forever *while the gate still appears to work*. The unchanged-accept check is therefore always submitted with real CRLF endings
- 04-02: decision controls render only when **`!advance.auto`** — the condition is "this page is not about to resubmit itself", not "the run finished". A halted run's written drafts are still decidable, and a 400ms self-submit under a half-typed edit would discard it. Passed into `DraftSection` as one `interactive` prop, never re-derived inside the component
- 04-02: the textarea prefills from **`final_body ?? body`**, a rejected draft still renders its post with `final_body` NULL, and `body` is never written by any path. Re-verified after every check: unchanged on all five seeded rows
- 04-02: route shape is `RUN_ID_PATTERN` + `DRAFT_ID_PATTERN` (`/^\d{1,9}$/`) → 400; `decision` must be exactly `accept` or `reject`, never inferred from a missing field → 400; empty or >`MAX_DECISION_BODY_CHARS` (5000, LinkedIn's own ceiling is 3000) → 400; a draft with no post yet → 400; `recordDecision` false → 404; then 303 back to the run. Reject skips `getRunView` entirely (one query, not four) and still 404s on a cross-run draft
- 04-02: the run list shows **`1 accepted · 2 edited · 3 —`** per draft position (APPR-06 asks per draft, not a tally). An undecided or unwritten draft is `—`, never "pending" — pending is Phase 3's job vocabulary and would say the engine is still working. No new CSS; `.hint` carries the cell

- 04-03: **`buildDraftingInput(samples, approvedPosts, transcriptBody, template)`** — `approvedPosts` is the SECOND parameter so argument order matches block order (`<approved_posts>` renders between `</voice_samples>` and `<transcript>`, inside the cacheable prefix). The two adjacent `string[]`s are indistinguishable to the type checker, so a test passes distinctly recognisable strings and asserts each lands in its own block. That test is the only thing between a refactor and a silent swap that would compile, run, and draft from the wrong material
- 04-03: the block **renders even when empty**, same rule and reason as the voice samples — the prefix before `<format>` must be byte-identical across a run's three drafting calls or drafts 2 and 3 stop being cache-eligible. Caps are named constants beside `MAX_SAMPLES`: `MAX_APPROVED_POSTS = 3`, `MAX_APPROVED_CHARS = 2500` (deliberately the same figure as `MAX_SAMPLE_CHARS` — an approved post is the same kind of object as a voice sample)
- 04-03: **approved posts are voice, never grounding.** `checkGrounding`'s pool stays `[transcript.body, ...sampleBodies]` and the reason is written at the site of the omission: approving a post for publication is not certifying its claims (run 1's draft 2 carried an invented slogan), so crediting one as a source would let a fabrication launder itself into a permanent source re-credited in every later run. Same reason outlier bodies have never been in the pool. `DRAFT_INSTRUCTIONS` says the same thing to the model, and adds a repetition rule — no reusing an approved post's topic, opening line, example or phrasing — because "every post sounds like last week's" is the failure mode this block creates
- 04-03: the call site is `listApprovedPosts(c.env.DB, MAX_APPROVED_POSTS, runId)` — the constant from `./prompts`, the query from `./db`, the route the only place that knows both, so `src/db.ts` still imports nothing from `src/prompts.ts`. `runId` as the exclusion is load-bearing: without it a mid-run approval feeds draft 1 back into draft 3 and the cacheable prefix stops being byte-stable. Step route budget is now at most 11 D1 queries (of 50) and no extra subrequest
- 04-03: **an approved checkpoint with no reported observations is recorded as approved-but-unobserved, never as verified.** The user can approve; they cannot thereby make an observation nobody made. Writing "verified on production" into STATE or a SUMMARY is a false statement in the one document the next plan reads first

- 05-01: **`src/zernio.ts` is the publishing boundary**, standing to CLAUDE.md's auto-publishing ban exactly as `src/prompts.ts` stands to the Jersey/JFSC one. Token is an argument, never an environment read — and a grep of the file for `c.env` / `process.env` returns **nothing, comments included**, which is why the plan's own suggested comment naming the binding was reworded. A rule a verifier can grep beats a rule well phrased in prose
- 05-01: **`isDraft: true` is sent explicitly, never inferred from absence.** Zernio's spec offers both routes to a draft (set the flag, or omit `publishNow` / `scheduledFor` / `queuedFromProfile`), and its precedence rule says the flag wins over the other two. Absence is a property a refactor destroys silently while adding an unrelated feature; the flag survives it. `platforms` is sent too, though a draft may omit it — a draft with no target account is one click from being scheduled inside Zernio's own UI
- 05-01: **the safety test was proved by mutation, not by assertion.** A test asserting a field is absent passes trivially against a file that never had it. Three mutations were applied and reverted SHA-identical: deleting `isDraft` fails 2, adding `publishNow: true` fails 3, and nesting `scheduledFor` inside `platforms[]` fails 3 — **the third is the one a `not.toHaveProperty` check alone would have missed**, and it is why the plan asked for a `JSON.stringify` assertion and a source-level scan as well. Any future edit to `buildDraftRequest` should be re-proved the same way
- 05-01: **a 409 duplicate is a success, not a failure.** Zernio hashes `(platform, accountId, content)` for 24h and answers 409 with `details.existingPostId`; a 200 with `existingPost` is the separate `x-request-id` replay layer (~5 min). Both yield a post id, so pressing a push button twice cannot create two LinkedIn drafts and never reads as broken. `x-request-id` is fresh per call — reusing one would make every later call look like a retry of the first
- 05-01: **`sk_`, not `sk-`, and the resemblance is a trap.** `scrubKey` in `src/runs.tsx` strips OpenAI-shaped `sk-` fragments and would pass a Zernio key (`sk_` + 64 hex) straight through. `src/zernio.ts` scrubs `/sk_[A-Za-z0-9_*-]+/g` on every Zernio message before it leaves the module, so no caller has to know which shape it holds
- 05-01: **length is the caller's check.** `MAX_LINKEDIN_CHARS = 3000` is exported and the client does not enforce it: `MAX_DECISION_BODY_CHARS` is 5000, so an accepted draft can legally sit in D1 at a length Zernio rejects. Checking before the request saves a round trip, and truncating inside the client would push text the executive never approved. `dryRun` (TikTok-only, 400 without a `tiktok` entry) and `timezone` (only interprets a schedule time this client never sends) are absent by decision and named in comments so nobody re-reaches for them
- 05-01: the fetch paths are **deliberately untested**, matching `src/fireflies.ts` and `src/openai.ts` — pure logic is pinned, network calls are verified by driving the real thing at 05-04's checkpoint

- 05-02: **migration 0006** adds `drafts.zernio_post_id`, `zernio_pushed_at`, `zernio_error_code`, `zernio_error_message`, all nullable, applied to local **and remote** D1. A **NULL post id beside a non-NULL error code is a failed push and a real state** — SCHED-03 asks for the pushed state *and* a clear error, so the page must distinguish "never pushed" (all NULL) from "tried and failed". Written into the migration header and onto the `DraftView` type, because it is invisible in the column list
- 05-02: **the success/failure asymmetry is the state machine.** `recordPush` clears both error columns (a draft that failed once and then succeeded is a pushed draft; stale error text beside a live post id renders as both at once); `recordPushFailure` leaves `zernio_post_id` alone (a failed re-push must not discard the receipt for a post that still exists in Zernio). Symmetry in either direction produces a lie. `zernio_pushed_at` is the **last attempt**, success or failure
- 05-02: the migration header states that **nothing in it is ever published** — the id is a receipt for an unscheduled draft inside Zernio, and published posts carry `platformPostUrl`, which this project never creates. Same discipline as `src/zernio.ts`: the ban is written where the thing lives, not only in policy docs
- 05-02: ownership is **still** the WHERE clause, copied from `recordDecision` deliberately; the error message reuses the existing `MAX_ERROR_MESSAGE` (500) rather than adding a second cap to keep in step; and the four columns ride in the `getRunView` projection that already ran, so the run page did not buy a read. **`listRuns` was left alone on purpose** — SCHED-03 is satisfied on the run page, and push state in the list is a wider read for no requirement
- 05-02: `src/db.ts` still imports **nothing at all**, now also from `./zernio` — the post id and error code arrive as caller-supplied primitives, exactly as `limit` does from `src/prompts.ts`
- 05-02 (method, worth reusing): helper SQL was verified by **capturing it from the compiled module through a stub `D1Database`** and running the captured statement verbatim against local D1, rather than retyping the query into a seed script. The cross-run case is the security claim of the plan, and a hand-copied `WHERE` clause that happens to be right proves nothing about what ships. The same capture turned the 500-char cap into an observation (a 700-char message arrived at the binding as 500)

- 05-03: **the connection check is the list call.** Zernio has no `/me`, so `GET /v1/accounts` proves the key *and* returns the `accountId` a push needs — one request, the `/fireflies` pattern. `/zernio` is the only file that reads the `ZERNIO_USER_TOKEN` binding; `src/zernio.ts` still greps clean for `c.env`
- 05-03: **the form is not evidence.** `POST /zernio` shape-guards the id (`/^[a-f0-9]{24}$/i` → plain-text 400 before D1), then **re-lists the accounts and refuses any id Zernio does not return**. Observed: the previously stored real id was rejected 400 while a stand-in listed a different account. Trusting the form would store an id that fails much later, on a push, as a Zernio 403
- 05-03: **zero connected accounts is a `.notice.warn`, not an error** — the key works, there is simply nowhere to push yet, and red would send someone hunting a fine key. An **inactive** account is listed and still selectable with a reconnect hint, because hiding it loses the account the user came to find. A **stored id that is no longer in the list** gets its own warn (a disconnect/reconnect in Zernio changes `_id`, and a silent non-match would push to an id this key no longer owns)
- 05-03: a **401 gets an extra paragraph** naming `.dev.vars` and `npm run secrets:push`. `zernio.account_label` is stored beside the id so a run page can name the target **without spending a Zernio request per render** — the 03-06 instinct. Both settings keys live in `src/db.ts`'s Phase 5 section rather than beside `SPEAKER_NAME_KEY`, which sits under the Phase 2 header
- 05-03 (method, worth reusing): **"no request was made" is measurable.** wrangler's local observability API records a span per outbound `fetch` with `url.full`; the count of `zernio.com` spans was 7 before and 7 after a GET and a POST with the token removed. The same table showed the real call's URL and that the `Authorization` header is not captured in the trace. And the three states the real API will not produce on demand (empty list, inactive account, 503) were rendered by pointing `ENDPOINT` at a local stand-in, then restoring `src/zernio.ts` SHA-identical (`f2f6273…`, `git diff` empty)

### Pending Todos

- **Phase 4, high — the draft-quality findings from 03-04's real run. Two of the three are still open, and they interact:**
  - ~~`.planning/todos/pending/normalise-grounding-match.md`~~ — **CLOSED by 03-07**, moved to `.planning/todos/done/`. Built and calibrated in 03-05, wired to `finishDraft` and the run page in 03-07, `isGrounded` deleted, deployed. It fixed the *instrument*; it did not make a draft better
  - `.planning/todos/pending/steer-draft-topics.md` — no topic input anywhere, and the three drafting calls run blind to each other. **Still open, and now the largest of the three.** 04-03's approved-posts block does not touch it: the block is identical across a run's three calls, so it cannot stop them colliding on one topic. It adds a "do not reuse an APPROVED POST's topic" rule, which is about last week's posts, not this run's siblings
  - `.planning/todos/pending/context-layer-for-drafts.md` — live context retrieval. Recorded only; the user decided 2026-09-15 not to insert it as a phase yet. **Still open**
- ~~Phase 4 (small): `usage_json` does not capture `usage.input_tokens_details.cached_tokens`~~ — **closed by 03-07**, folded into `finishDraft` as STATE said it should be. `toUsageJson` records it and `src/openai.ts`'s `Usage` declares it; one more run settles 03-04's "caching eligible but unproven" note
- ~~Phase 4 (small): `excerptTranscript` returns `linesUsed`/`linesTotal` and no page renders them~~ — **closed by 03-06.** The counts are stored on the run at creation and rendered on the run page; a transcript that will be cut is flagged on the new-run form before the run is paid for. Run 1's own coverage stays permanently unknown (it predates the columns)
- Still pending (low, hardening): narrow the transcript prop on the Fireflies import preview — see `.planning/todos/pending/narrow-transcript-prop-type.md`. It said to pick this up in any Phase 3 plan touching `fireflies-routes.tsx`; no plan in Phase 3 or Phase 4 touched that file, so it carries to Phase 5 or the next plan that opens it

- ~~Phase 5: re-research and re-plan against the Zernio API before planning that phase~~ — **closed**: `.planning/phases/05-zernio-push/DISCOVERY.md` reads the authoritative OpenAPI 3.1 spec (v1.0.4), and Phase 5 is planned as 05-01 … 05-04. Note the doc's own warning: `docs.zernio.com` contradicts the spec on the draft field (it shows `publishNow: true` and a snake_case `is_draft`), and the spec wins

### Blockers/Concerns

- Phase 1 (resolved in 01-02): Zero Trust enabled and the Worker protected via the dashboard; `ctx.access` confirmed working, no jose fallback
- Phase 2 (02-03, closed): the Access policy check was step 1 of the 02-03 verification. The user confirmed the checkpoint, including that step; whether it required a change was not reported
- Phase 2 (02-02, minor): `/sources` loads every transcript summary and every sample body in one page render; fine for one executive at pilot scale, needs DB-level paging/truncation if the archive grows
- Phase 2 (02-03, ongoing): Fireflies is US-hosted. Storing only the executive's own lines is the mitigation, not a resolution, of the Jersey/GDPR transfer question in CLAUDE.md
- Phase 2 (02-03, minor): `/fireflies` shows one page of 50 meetings with skip paging only — no search or date filter. Fine at pilot scale
- Phase 3 (closed by 03-RESEARCH.md, 2026-09-15): the Worker-timing worry was misdirected. Cloudflare states waiting on `fetch()` does not count toward CPU time and HTTP-triggered Workers have no hard duration limit. The real constraints are the Free plan's 10ms CPU per request (spent marshalling payloads, not waiting), `ctx.waitUntil` capping at 30s, and DRAFT-05 needing visible status. Resolved by one OpenAI call per invocation driven by a D1 job table; no queue, Durable Object, Workflow or paid plan needed
- Phase 3 (compliance, from research): every OpenAI request must set `store: false` — the Responses API default is 30-day application-state retention. Abuse-monitoring logs are still kept 30 days and need a ZDR agreement to change; state this honestly rather than claiming no retention
- Phase 3 (compliance, from research): the prompt builder must take primitives only, never a TranscriptRow — Fireflies meeting titles routinely name the counterparty, so passing the row would send a client identifier to OpenAI. Assert it in a test
- Phase 2 (verification, info): `POST /sources/samples` has run locally but never against production D1 (remote `voice_samples` is empty); the deployed bundle is identical, so this is usage-not-yet-occurred, not a gap
- Phase 3/4 (03-02, 04-01, info): `npm run db:migrate:remote` fails with Cloudflare API error 7403 ("account not valid or not authorized") on a valid token with `d1 (write)`, then succeeds on an immediate retry. **Two occurrences in four remote migrations** (03-02 and 04-01; 03-06 and 05-02 both went through on the first attempt), so this is a recurring transient, not a one-off. Always retry once before re-authenticating
- **Phase 3 (03-04, THE open risk of this project): draft quality does not clear the bar.** Run 1 produced three drafts the user would NOT publish with light edits. The 80% target is unmet with exactly one data point behind it. The engine is verified and is not the problem — the three findings are: a grounding check that passes invented material (**closed by 03-05/03-07**), three drafts colliding on the same topic with no topic input anywhere (**open**), and a ceiling of "common sense" because one meeting transcript is the only substance source (**open**). **Phase 4 must not be called a success while the drafts stay unpublishable**; the approval gate will faithfully record a low rate, which is the point, but the todos are what move it. Note what the closed one bought: the gap-closure wave fixed the *instrument*, not the output — the next run produces drafts of the same quality as the last, better described
- **Phase 3 (03-05, acted on by 03-07): `grounded` is `false` on all three production drafts, and correctly so.** A boolean that is false on 3/3 real drafts is exactly as uninformative as one that is true on 3/3. **The tick is not the UI** — 03-07 renders the report (`supported / checked`, the named `unsupported` lines, the `repeated` phrases, and `skipped`) and never the bare boolean. Keep it that way in Phase 4's approval gate
- Phase 3 (03-05, evidence): the plan expected draft 1 to flip to `grounded: true`. It did not, and that is the right answer — its citations do now resolve, but the post asserts "between 20% and 40%" and `20%` appears **zero times** in the executive's material. Draft 3 also moved from stored `1` to `false`. Reported rather than tuned away, per the plan's own instruction; no threshold in the 0.25-0.5 range makes any draft fully supported
- Phase 3 (03-05, corroborates the open risk): at 4-token granularity the **median** sentence overlap between run 1's drafts and the executive's own material is **0.00**. The drafts are the model's constructions, not the executive's words rearranged. Same finding as `context-layer-for-drafts.md`, arriving from a different direction. One production run is three data points — re-check once the approval gate has produced more
- Phase 3 (03-06, resolved by 03-07): production was running pre-0004 code against a post-0004 schema. 03-07's deploy (version `f17cf55d`) ships both the coverage UI and the grounding panel; code and schema are back in step
- **Phase 3 (03-07, still open after 04-03): the grounding panel has never been seen against a real draft.** All six render states were verified against hand-seeded local D1. The 03-07 checkpoint was approved without spending ~$0.10 on a fresh run, and the 04-03 checkpoint asked for the same observation and was approved without it too. The judgement that actually tests the check — are the amber lines genuinely not the executive's words, and are the clean drafts genuinely theirs — is unmade. 03-05's three-draft calibration is the only evidence
- Phase 3 (03-07, still open after 04-03): **`/health` has not been observed green since version `b3156846`** — now three deploys back (`b3156846` → `f17cf55d` → `734bc0e6`). It cannot be checked from the CLI: Cloudflare Access 302s an unauthenticated request at the edge, before the Worker runs (correct, fail-closed behaviour). Both the 03-07 and 04-03 checkpoints asked for it and both were approved without it being reported. The D1 round-trip and the three secrets on `734bc0e6` are approved-but-unobserved. Thirty seconds in a browser before the next paid run; any *scripted* health check would need an Access service token, which this project does not have
- Phase 3 (03-07, info): run 1's coverage line rendering was also left unobserved. It predates migration 0004, so "nothing at all" is the expected and correct output
- Phase 3 (03-07, info): every production draft so far has a NULL `grounding_json` and shows "written before the current grounding check". The first run after this deploy is the first draft anywhere with a real report
- Phase 3 (03-04, resolved): `OpenAIError.retryable` is not stored on the job row — resolved by the default-deny `RETRYABLE_ERROR_CODES` map in `src/runs.tsx`; no column added
- Phase 3 (03-03, resolved by 03-04): the run page's "Nothing has run yet" notice has been rewritten now that an engine exists
- Phase 3 (03-04, info, now measurable): prompt caching was **eligible** on drafts 2 and 3 (byte-stable ~4,700-token prefix, well over the 1,024 minimum) but could not be proven from D1. 03-07 stores `cached_tokens`, so the next run settles it. Rows written before that commit have no key at all — absence is unknown, not zero
- Phase 3 (03-04, info): CPU ms per step was not captured. Indirect evidence only — six steps on the Free plan with the largest payload the input caps allow, no Error 1102 `exceededCpu`
- Phase 3 (03-03, minor): `/runs` lists every run with no paging, like `/sources`. Fine at pilot scale, but `listRuns` counts jobs per run with subqueries, so a long history would want a limit
- Phase 3 (03-03, resolved by 03-06): `POST /runs` loaded the whole transcript body only to prove the row existed and then discarded it. That read now pays for the coverage counts stored on the run
- **Phase 4 (04-02, still open after 04-03's deploy): the gate has never been used on a real draft.** All eight plan checks and eight rejection paths were driven through the live route against seeded local D1, but every decision recorded so far is one an agent made about text an agent wrote. **No decision has ever been written to production D1.** 04-03 deployed the code, so the blocker is no longer the deploy — it is one browser session. The first reading of the approval rate is the executive's first session with it
- Phase 4 (04-02, info): `/runs` still could not be checked in production for the usual reason — Access 302s an unauthenticated request before the Worker runs. Everything in 04-02 is verified locally only
- Phase 4 (04-02, confirmed by 04-03): a low approval rate starves the feedback loop. `listApprovedPosts` reads only `accepted` and `edited` rows, so with nothing approved the `<approved_posts>` block renders **empty** and the prompt is byte-for-byte Phase 3's. The loop starts helping only once something clears the gate

- **Phase 4 (04-03, THE process concern): this is the SECOND consecutive checkpoint approved without the observations being reported** (03-07, then 04-03). The unobserved set is **growing rather than closing** — 04-03's checkpoint deliberately re-asked for 03-07's three outstanding items and added a fourth, and came back "approved" with nothing reported. Currently approved-but-unobserved: (1) `/health` green, now across three versions; (2) the accept / edit-then-accept / reject round trip on production D1; (3) the grounding panel against a real draft and the human judgement of whether the amber lines are genuinely not the executive's words; (4) DRAFT-04 end to end. **One browser session plus about $0.10 still closes all four at once.** Until then, no plan may describe any of them as verified
- **Phase 4 (04-03, open): DRAFT-04 is wired, not working.** Criterion 5 is unit-tested (61/61) and payload-verified against a request body captured from the real code path against a local stand-in endpoint — correct block order, another run's approved post present, the run's own accepted draft absent, no meeting title or speaker label. **No run has been executed with an approved post in the prompt.** Whether drafts written with an accepted post in front of them read closer to publishable is unknown, and so is whether the three drafts of a run still collide on one topic. Record it as wired, unit-tested and payload-verified — never as working

- **Phase 5 (05-03, CLOSED — the roadmap's open plan question is answered): the Zernio API works on this plan.** `GET /v1/accounts?platform=linkedin&status=connected` returned **200** with one connected LinkedIn account (`Vincent Sider`, id `YOUR_ZERNIO_ACCOUNT_ID`, `isActive: true`). No 402, no `free_tier_exceeded` — the documented free tier covers one account and this is one account. Both 05-03 `user_setup` prerequisites are **verified rather than assumed**: LinkedIn is connected inside Zernio, and `ZERNIO_USER_TOKEN` in `.dev.vars` holds a working Zernio API key. This says nothing about `POST /v1/posts`, which is the call that can put text on a real profile
- **Phase 5 (05-03, new): the production Worker secret has not been checked.** `.dev.vars` holds a key proved good locally; `/health` reports presence only, and 05-03 deployed nothing. If the Worker secret was pushed with a different value, remote `/zernio` will render the 401 page — which is exactly the state the page was built to explain. First remote load is the check, and it belongs to 05-04
- Phase 5 (05-03, observed, previously spec-only): a bad key returns **401** with `{"error": "Unauthorized"}` and **no machine-readable `code`**, so `toZernioError`'s `http_<status>` fallback is what renders (`code http_401`). `_id` really is a 24-char hex ObjectId. Zernio's LinkedIn `username` is a display name, not a handle — it equalled `displayName` on the real account, so the `(@handle)` suffix is now conditional
- Phase 5 (05-01, narrowed; the plan half is now closed by 05-03): whether the Zernio API is available on Vincent's plan **cannot be answered from documentation** and no further reading will settle it. `GET /v1/accounts` returning his LinkedIn account is the answer, it costs nothing, and `listLinkedInAccounts` is that call. The documented free tier covers one connected account (`free_tier_exceeded` fires above two without a card), and `createPost` does not list a 402 at all. Two prerequisites are 05-03 `user_setup`: LinkedIn connected in the Zernio dashboard, and `ZERNIO_USER_TOKEN` holding a Zernio **API key** (`sk_` + 64 hex) rather than some other token
- **Phase 5 (05-01, half-closed by 05-03): the read path has now reached Zernio; the write path has not.** `listLinkedInAccounts` has been exercised against the real service on both a good key (200) and a bad one (401), and both behaved as the spec said. **`createLinkedInDraft` has still never been called** — the 201 create, the 200 `x-request-id` replay, the 409 duplicate and every 403 code remain implemented-from-spec and unobserved. The 401 being right first time is evidence the spec was read correctly; it is not evidence about the call that can publish. Original wording follows, and its warning stands unchanged for 05-04: no network path had run — not once, not against a bad token. The 200 `x-request-id` replay, the 409 duplicate and the 429 are implemented from the spec and **never observed**. 05-04's checkpoint is first contact with a service that can publish, and this project's standing record is two consecutive checkpoints approved with no observations reported (03-07, 04-03). **That cannot happen a third time here**: unlike `/health`, an unobserved publishing client is a risk to Vincent's real LinkedIn profile, not just an unverified claim
- Phase 5 (05-01, minor): the 429 path throws a plain `ZernioError` with code `http_429`. Zernio sends `X-RateLimit-Limit` / `-Remaining` / `-Reset` on every response and the client reads none of them; its own documented ceilings are 25 posts/hour per account plus account cooldowns. Fine for one executive pushing a handful of drafts — if a caller ever needs to pause, that is where the number lives. `GET /v1/posts/{postId}` (confirm a draft landed) is also unimplemented; nothing needs it yet

- **Phase 3 (re-verification, 2026-09-15): status is `human_needed`, not `passed`.** Nothing is missing, stubbed or unwired, and both original gaps are independently re-proved closed — the decisive probe (three genuine citations wrapped around a fully invented body) used to pass and now returns `grounded: false` with all three fabricated sentences quoted back. But three things have never been observed live, and one ~$0.10 run closes all three at once:
  1. The grounding panel against a real draft — every render state was seeded with hand-written JSON
  2. The write-path round trip. `toGroundingJson` (writer, `src/db.ts`) and `parseGrounding` (reader, `src/runs.tsx`) are both module-private, there is no test file for `src/db.ts`, and seeding validates only the reader. All nine field names and types were confirmed to agree **by reading both sides**, so the failure that would matter (a renamed key rendering every draft as "no report") is ruled out by inspection, not by execution
  3. `/health` green on version `f17cf55d` — unreachable from an agent, since Access 302s an unauthenticated request before the Worker runs (`service_token_status: false`, `auth_status: NONE`). The gate is correctly fail-closed; that is also why the live run page cannot be checked from here
- Phase 3 (verification, judgement recorded): criterion 3 is about **compliance, not draft quality**. The code now confines drafts to the executive's own material and reports honestly on how well it managed, so DRAFT-03 moved PARTIAL → SATISFIED. Run 1's unpublishable drafts are a Phase 4 problem and stay open. Conflating the two would either fail Phase 3 for something it never promised, or let the quality problem be marked closed because a grounding check exists
- Phase 3 (documented blind spot, by design): a short invented sentence said once lands in `skipped`, not `unsupported` — the check matches words, not meaning. The panel prints "N short or connecting lines were too generic to check" in **every** state, so a clean result cannot be read as a purity guarantee

- **Phase 4 (verification, 2026-09-15): status is `human_needed`, not `passed` — and the reason is identical to Phase 3's.** Nothing is missing, stubbed or unwired; 16/17 must-haves verified in code, 0 failed. The one open item is that **four things have never been observed on production**, and one browser session plus about $0.10 closes all four:
  1. `/health` green on version `734bc0e6` — now unobserved across three consecutive deploys (`b3156846`, `f17cf55d`, `734bc0e6`)
  2. The accept / edit-then-accept / reject round trip against production D1. **No decision has ever been written to the production database.** The verifier's own note: this is the only test that would expose a CRLF fault against a real browser — a curl check would not, because the CRLF-vs-LF mismatch only arises from an actual form submission
  3. The grounding panel against a real draft, and the judgement of whether the amber lines are genuinely not the executive's words
  4. DRAFT-04 end to end — wired, unit-tested and payload-verified against a captured request body, but **no run has ever executed with an approved post in the prompt**, because nothing has cleared the gate yet
- **Phase 4 (process, the compounding one): 03-07 and then 04-03 are two consecutive checkpoints approved without any observation being reported.** The unobserved set is growing, not closing — 04-03 deliberately re-asked for 03-07's three items and added a fourth. Every one of them is cheap to close and none of them is code work. If Phase 5 is approved the same way, the project will have a fully "complete" pipeline that nobody has watched run
- Phase 4 (verification, judgement recorded): the phase was **not** failed for draft quality, and that is deliberate. Phase 4 promised that decisions are recorded and fed back; both are done, and a gate that faithfully records a low approval rate **is the gate working**. But completion here must not be read as closing the 80% problem — it remains unmet and untested since run 1, and DRAFT-04 is the one lever in this phase that could move it, unexercised
- Phase 4 (04-03, structural limit worth remembering): the approved-posts block **cannot** help `steer-draft-topics.md`. It is byte-identical across a run's three drafting calls, so it carries voice but supplies nothing that could stop the three drafts colliding on one topic. And while nothing is approved the block renders empty, which means the drafting prompt is currently byte-for-byte Phase 3's

## Session Continuity

Last session: 2026-09-15
Stopped at: Completed 05-03 — `/zernio`, the connection page, which made the project's first real Zernio request and came back 200 with Vincent's connected LinkedIn account. Only 05-04 (push from the run page, deploy, checkpoint) remains in Phase 5. Phase 4 remains code-complete and deployed as version `734bc0e6`, with nothing in it observed on production. Earlier history below.

05-03 (2026-09-15, Phase 5 wave 2): the connection page and account choice (`0273042`), the mount and nav (`31a2eff`). One file created (`src/zernio-routes.tsx`, 251 lines), three modified; `npm run check` 0, `npm test` 68/68 unchanged. **First contact with Zernio**: 200, one account, `isActive: true`, and the plan's two `user_setup` items turned out to be already satisfied, so no checkpoint was needed. Seven states observed rather than reasoned about — no key (500, and **0 outbound requests measured** by counting `zernio.com` fetch spans in wrangler's local observability API, 7 → 7), bad key against the real API (502, `code http_401`), connected-with-accounts (real API), and empty / inactive / 503 against a local stand-in with `ENDPOINT` repointed and `src/zernio.ts` restored SHA-identical. POST paths: bad shape, missing field and an unknown-but-well-formed id all 400; valid → 303 with both settings rows written; no `Origin` → 403. `.dev.vars` edited twice and restored SHA-identical (`a29cd2a0…`), the backup deleted afterwards. Two small auto-fixes: the account line no longer renders "Vincent Sider (@Vincent Sider)", and a stored account missing from the list now warns instead of silently preselecting nothing. **Not deployed** — 05-04 owns it

05-01 (2026-09-15, Phase 5 wave 1, ran in parallel with 05-02): the client (`34930d3`), the publish-safety test (`0c700ab`). Two files created, none modified; `src/db.ts` and `migrations/0006_push.sql` were left alone throughout because 05-02 owned them in the same wave, and both commits staged a single file by name. `npm run check` 0, `npm test` 61 → 68. Verified by mutation rather than by assertion alone — deleting `isDraft: true` fails 2 tests, adding `publishNow: true` fails 3, and nesting `scheduledFor` inside `platforms[]` fails 3; `src/zernio.ts` restored SHA-identical after each and `git diff --stat` confirmed empty. Fully autonomous, no checkpoint. **No Zernio request has been made**: every status code the client handles is read from the OpenAPI spec, not seen
05-02 (2026-09-15, Phase 5 wave 1, ran in parallel with 05-01): migration 0006 (`750c283`), the push writes and the widened run read in `src/db.ts` (`c5d6644`). Two files; `src/zernio.ts` and `test/zernio.test.ts` were left alone throughout because 05-01 owned them in the same wave, and both commits staged a single file by name. Applied to local **and remote** D1 on the first attempt (no 7403 this time). `npm run check` 0, `npm test` 68/68 (the extra 7 are 05-01's; this plan adds none). Verified by compiling `src/db.ts` with esbuild and calling both helpers against a stub `D1Database` that captured their SQL and bindings, then running those captured statements verbatim against two seeded local runs: failure → success → re-push failure in sequence (the success cleared `duplicate` to NULL and set `zp_abc123`; the later `http_502` was written **beside** that id, not over it), **`changes() = 0`** for both writers with a draft id from the other run and neither row's stored values moving, `body` untouched at every step, a 700-char message arriving at the binding as 500, and `getRunView` still issuing **3** queries. All seed rows deleted; local D1 confirmed back to zero. **Not deployed** — 05-04 owns it

Phase 3 complete. 03-04 — step route (`e6b363d`), auto-advance and retry (`160aa34`), voice-sample warning (`101c820`), deployed as version `b3156846-e6bb-4e4a-a14a-ad45b629406b`. The whole paid path was verified for free against a deliberately invalid key before the checkpoint; `.dev.vars` was backed up and restored SHA-identical, and local D1 is back to zero rows. The user then ran it on real data: 6/6 steps done first attempt, $0.097, three drafts they would not publish. Next: **Close the four unobserved items before planning Phase 5.** One browser session on https://pipeflick.your-subdomain.workers.dev: `/health` (three deploys overdue), then `/runs` → run 1 to accept one draft unchanged, edit-then-accept another and reject the third — that writes the first real decisions to production D1 and is the only check that would catch a CRLF fault. Then one ~$0.10 run to see DRAFT-04 actually work. After that, **Phase 5 (Zernio Push) needs re-research and planning** — the doc rename is done, the API research is not. The two quality todos (`steer-draft-topics.md`, `context-layer-for-drafts.md`) stay open and Phase 4 did not move them

03-06 (2026-09-15, gap closure wave, ran in parallel with 03-05): migration 0004 (`09d4adf`), coverage stored and exposed in `src/db.ts` (`75d37e5`), computed and rendered in `src/runs.tsx` (`acf51cf`). Migration applied to local **and remote** D1; **not deployed** — 03-07 owns the deploy. Verified against seeded local D1 (148 of 250 lines on a long transcript, "all 40" on a short one, nothing at all on a seeded pre-0004 run), then all seed rows deleted and local D1 confirmed back to zero. `src/prompts.ts` untouched, as 03-05 owns it this wave
03-05 (2026-09-15, gap closure wave, ran in parallel with 03-06): failing tests first (`15c9e90`), `checkGrounding` (`0568bd1`), calibration notes and the blind-spot test (`8ff671d`). TDD, 34 → 60 tests, `tsc` clean, `src/prompts.ts` still zero imports. Calibrated against production run 1 by exporting the three drafts, the 18,429-char transcript and the three voice samples to the scratchpad; all of it deleted afterwards and nothing but verdicts and counts recorded. Both known failures flipped: draft 1's three citations now resolve, and draft 2's invented slogan is the only phrase flagged across the batch. All three drafts come out `grounded: false`, which inspection says is correct. `migrations/`, `src/db.ts` and `src/runs.tsx` untouched, as 03-06 owned them this wave

03-07 (2026-09-15, gap closure wave 2): report stored and proxy retired (`dea4cc5`), whole-post check called and rendered (`330ca88`), deployed as version `f17cf55d-e6e8-4b04-bbaf-600fba5e7c93`. `isGrounded` deleted from `src/` and `test/`; `npm run check` 0, `npm test` 57/57; `src/db.ts` still imports nothing. All six render states driven against a hand-seeded seven-draft run in local D1 — legacy-null, clean, five capped unsupported lines with "and 7 more", one repeated phrase, `grounding_json` set literally to `not json` (page still 200), unresolved citations, and an unwritten draft — then every seed row deleted and local D1 confirmed back to zero. The `cached_tokens` todo was folded into `finishDraft` as STATE directed. **Checkpoint approved without the individual observations being reported**: `/health`, run 1's coverage line, and the panel against real output are approved-but-unobserved, not verified

04-01 (2026-09-15, Phase 4 wave 1): migration 0005 (`a4b26d2`), decision write and approved-post read in `src/db.ts` (`3113e2a`). Applied to local **and remote** D1 (remote needed one retry past API error 7403). **Not deployed** — 04-03 owns the deploy, so remote schema is deliberately ahead of remote code. `npm run check` 0, `npm test` 57/57 (the plan's "60" was stale; 03-07 deleted three tests), `src/db.ts` still imports nothing. Verified by running each helper's exact SQL against two seeded local runs: `changes() = 1` on accept/edit/reject, **`changes() = 0`** for a draft id from another run with its stored text unchanged, `body` intact beside a rewritten `final_body`, the approved read newest-first and excluding the given run, and the APPR-05 query returning `accepted n=2 delta=0 / edited n=1 delta=19 / rejected n=1 delta=NULL`. All seed rows deleted; local D1 confirmed back to zero

04-02 (2026-09-15, Phase 4 wave 2): decision form and route (`020aea0`), decisions in the run list (`f7af26a`). One file, `src/runs.tsx`; `src/db.ts` untouched, which is what keeps `/runs` at two queries by construction. `npm run check` 0, `npm test` 57/57 unchanged, `grep "draft.grounded"` and `grep "read-only"` both empty. Verified against `npm run dev` with two seeded runs (one finished, one mid-generation) plus a zero-draft run: all eight plan checks — including the unchanged-accept submitted with **CRLF** endings, which came back `accepted` with `final_body` byte-equal to `body` — plus bogus/missing decision values, bad ids, an unknown run, an unwritten draft, 5001 characters and a missing `Origin` (403 from `csrf()`). Cross-run accept *and* reject both 404 with the target row untouched. `body` re-compared against the seeded originals on all five rows afterwards: unchanged. Every seeded row deleted, local D1 confirmed back to zero. **Not deployed** — 04-03 owns it

04-03 (2026-09-15, Phase 4 wave 3): the approved-posts block and its tests (`d69581a`), the call-site read and the deploy (`aaf5241`). Three files; 57 → 61 tests, `npm run check` 0, `src/prompts.ts` still zero imports, `checkGrounding`'s pool untouched. **Deployed as version `734bc0e6-7963-4d70-8db8-349b27102d07`** (previous `f17cf55d`), confirmed at 100% by `npx wrangler deployments list`, which puts remote code back in step with migration 0005. Verified locally with a deliberately invalid `OPENAI_API_KEY` (the step reached the OpenAI call and failed on `invalid_api_key`, proving the read and the build ran; `.dev.vars` restored SHA-identical, `src/openai.ts` back to a zero diff), then by capturing the real request body against a local stand-in endpoint — block order `voice_samples` → `approved_posts` → `transcript` → `format`, another run's approved post inside the block, the run's own accepted draft absent from the whole payload, no meeting title or speaker label anywhere. Seed rows deleted, local D1 back to zero. **The checkpoint was approved with no observations reported, for the second time running** (03-07 was the first), so `/health`, the decision round trip on production D1, the grounding panel against a real draft and DRAFT-04 end to end are all approved-but-unobserved

Next: **05-04 is the last plan of the project, and it is the one that can publish.** Everything it needs now exists and is proved: a working key, a real account id in `settings` (`zernio.account_id` = `YOUR_ZERNIO_ACCOUNT_ID`), `createLinkedInDraft`, the four push columns, and `MAX_LINKEDIN_CHARS` to check a draft against before spending the request. Two things 05-04 must not skip: the production **Worker secret** has never been checked (only the local `.dev.vars` key has), and the first remote `/zernio` load is that check; and `POST /v1/posts` has still never been called by anything. 05-04 is **first contact with a service that can publish**, so it is the one checkpoint in this project that must not be approved without the observation being reported. Still outstanding from Phase 4, and still one browser session on https://pipeflick.your-subdomain.workers.dev: `/health`; run 1's drafts accepted / edited / rejected so a decision exists on production D1; the grounding panel read against real output; and, for about $0.10, one new run whose drafts were written with an approved post in the prompt, which is the only way to see DRAFT-04 do anything. The two quality todos (`steer-draft-topics.md`, `context-layer-for-drafts.md`) stay open and Phase 5 does not touch them — a scheduler moves approved drafts, it does not make them better
Resume file: None
