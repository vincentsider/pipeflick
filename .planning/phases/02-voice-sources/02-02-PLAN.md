---
phase: 02-voice-sources
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [migrations/0002_sources.sql, src/db.ts, src/settings.tsx, src/sources.tsx, src/index.tsx, src/layout.tsx]
autonomous: true

must_haves:
  truths:
    - "User opens /settings, saves a speaker name, and sees it prefilled after refresh"
    - "User pastes a past LinkedIn post on /sources and it appears in the voice samples list after redirect"
    - "/sources lists stored transcripts (title, date, duration, speaker, line count) and each links to a page showing its stored lines"
    - "Migration 0002 is applied locally and remotely; transcripts and voice_samples tables exist"
  artifacts:
    - path: "migrations/0002_sources.sql"
      provides: "transcripts and voice_samples tables"
      contains: "CREATE TABLE transcripts"
    - path: "src/db.ts"
      provides: "Typed D1 helpers: upsertTranscript, listTranscripts, getTranscript, insertVoiceSample, listVoiceSamples, SPEAKER_NAME_KEY"
      exports: ["upsertTranscript", "listTranscripts", "getTranscript", "insertVoiceSample", "listVoiceSamples", "SPEAKER_NAME_KEY"]
    - path: "src/settings.tsx"
      provides: "GET/POST /settings for the speaker name"
    - path: "src/sources.tsx"
      provides: "GET /sources, POST /sources/samples, GET /sources/transcripts/:id"
      min_lines: 60
  key_links:
    - from: "src/settings.tsx"
      to: "settings table"
      via: "setSetting(db, SPEAKER_NAME_KEY, value)"
      pattern: "setSetting\\(.*SPEAKER_NAME_KEY"
    - from: "src/sources.tsx"
      to: "src/db.ts"
      via: "listTranscripts + listVoiceSamples on GET, insertVoiceSample on POST"
      pattern: "insertVoiceSample\\("
    - from: "src/index.tsx"
      to: "settings and sources routers"
      via: "app.route after requireAccess and csrf"
      pattern: "app\\.route\\(\"/\", sources\\)"
---

<objective>
Give the executive's words a home: the D1 tables for transcripts and voice samples, the speaker-name setting, a paste form for past LinkedIn posts, and the sources view that lists everything stored (VOICE-03 setting half, VOICE-05, VOICE-06).

Purpose: Plan 02-03 needs `upsertTranscript` and the speaker-name setting to import from Fireflies, and Phase 3 reads transcripts and samples from these tables. Everything here is internal CRUD on established patterns.
Output: migration 0002 (applied local and remote), db helpers, `/settings`, `/sources`, `/sources/transcripts/:id`, `POST /sources/samples`, nav links in the layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-voice-sources/02-DISCOVERY.md
@src/index.tsx
@src/db.ts
@src/layout.tsx
@src/health.tsx
@src/access.ts
@migrations/0001_settings.sql
@wrangler.jsonc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0002 and typed D1 helpers</name>
  <files>migrations/0002_sources.sql, src/db.ts</files>
  <action>
    Create `migrations/0002_sources.sql`:
    - `transcripts (id TEXT PRIMARY KEY, title TEXT NOT NULL, meeting_date TEXT NOT NULL, duration_minutes INTEGER NOT NULL, speaker_label TEXT NOT NULL, body TEXT NOT NULL, line_count INTEGER NOT NULL, imported_at TEXT NOT NULL)` — `id` is the Fireflies transcript id; `body` holds only the executive's lines joined by "\n". Add a header comment: no other speaker's text, participant emails, summary or URLs are stored here, by design (PROJECT.md compliance constraint).
    - `voice_samples (id INTEGER PRIMARY KEY AUTOINCREMENT, body TEXT NOT NULL, created_at TEXT NOT NULL)`.
    - `CREATE INDEX transcripts_meeting_date ON transcripts(meeting_date DESC)`.

    Extend `src/db.ts` (keep getSetting/setSetting untouched, keep the "bound parameters only" rule):
    - `export const SPEAKER_NAME_KEY = "speaker.name"`.
    - `export type TranscriptRow = { id: string; title: string; meeting_date: string; duration_minutes: number; speaker_label: string; body: string; line_count: number; imported_at: string }` and `TranscriptSummary = Omit<TranscriptRow, "body">`.
    - `export type VoiceSampleRow = { id: number; body: string; created_at: string }`.
    - `upsertTranscript(db, row: Omit<TranscriptRow, "imported_at">)`: INSERT ... ON CONFLICT(id) DO UPDATE SET every column; sets `imported_at` to now.
    - `listTranscripts(db): Promise<TranscriptSummary[]>` (no body column; ORDER BY meeting_date DESC).
    - `getTranscript(db, id): Promise<TranscriptRow | null>`.
    - `insertVoiceSample(db, body): Promise<number>` (returns the new id from `meta.last_row_id`).
    - `listVoiceSamples(db): Promise<VoiceSampleRow[]>` ORDER BY created_at DESC.

    Apply: `npm run db:migrate:local` then `npm run db:migrate:remote` (wrangler asks for confirmation and auto-answers yes non-interactively, as seen in 01-01). Remote apply is additive and safe; it lets Plan 02-03 deploy without touching migrations.
  </action>
  <verify>
    `npm run check` exits 0. `wrangler d1 migrations list pipeflick-db --local` and `--remote` both report no migrations to apply. `wrangler d1 execute pipeflick-db --remote --command "SELECT name FROM sqlite_master WHERE type='table'"` lists `transcripts` and `voice_samples`.
  </verify>
  <done>Both tables exist locally and remotely; helpers compile and use only bound parameters.</done>
</task>

<task type="auto">
  <name>Task 2: Speaker-name settings page</name>
  <files>src/settings.tsx, src/index.tsx</files>
  <action>
    Create `src/settings.tsx` exporting `settings = new Hono<AppEnv>()`:
    - `GET /settings`: reads `getSetting(c.env.DB, SPEAKER_NAME_KEY)`, renders a form (`method="post"`, one text input `speaker_name` prefilled, maxlength 200) with the note "Fireflies labels you by the name it detected in the meeting, or 'Speaker 1' when it could not. Enter the name as Fireflies shows it; you can still pick a different speaker on each import." Show "Saved." when the query string has `?saved=1`.
    - `POST /settings`: `const body = await c.req.parseBody()`; take `speaker_name` as string, trim, collapse whitespace; reject > 200 chars with 400 text; save with `setSetting`; `return c.redirect("/settings?saved=1", 303)`. An empty value is allowed and stored as "" (clears the setting).

    In `src/index.tsx`: import `csrf` from "hono/csrf" and register `app.use(csrf())` right after `app.use("*", requireAccess)` (first POST forms arrive in this phase; Origin check costs nothing behind Access). Mount `app.route("/", settings)`. Add a `<a href="/settings">Settings</a>` link on the home page alongside Health (the layout nav is added in Task 3).
  </action>
  <verify>
    With `npm run dev` running: `curl -s localhost:8787/settings` returns 200 with the form; `curl -s -i -X POST -H "Origin: http://localhost:8787" -d "speaker_name=Vincent Sider" localhost:8787/settings` returns 303 to `/settings?saved=1`; a following GET shows `value="Vincent Sider"`. A POST without an Origin header from a different origin (`-H "Origin: http://evil.example"`) is rejected by csrf (403).
  </verify>
  <done>Speaker name round-trips through the settings table and is visible after refresh.</done>
</task>

<task type="auto">
  <name>Task 3: Sources view, paste samples, transcript detail, nav</name>
  <files>src/sources.tsx, src/index.tsx, src/layout.tsx</files>
  <action>
    Create `src/sources.tsx` exporting `sources = new Hono<AppEnv>()`:
    - `GET /sources`: two sections. "Transcripts": table of `listTranscripts` rows (title linking to `/sources/transcripts/:id`, meeting date formatted `YYYY-MM-DD`, duration `N min`, speaker label, line count, imported date); empty state "No transcripts yet. Import one from Fireflies." linking to `/fireflies` (built in 02-03; a 404 until then is fine). "Voice samples": paste form (`method="post" action="/sources/samples"`, `<textarea name="body" rows="8" maxlength="20000" required>`, button "Save sample") followed by the list of `listVoiceSamples` rows showing created date and the first 200 characters of each body plus a `<details>` with the full text. Show "Sample saved." when `?saved=1`.
    - `POST /sources/samples`: parseBody, `body` string, trim; empty → 400 "Paste some text"; > 20000 chars → 400; `insertVoiceSample`; redirect 303 to `/sources?saved=1`.
    - `GET /sources/transcripts/:id`: validate id against `/^[A-Za-z0-9_-]{1,100}$/` (400 otherwise); `getTranscript`; 404 "Transcript not found" if null; render title, date, duration, "Lines kept for speaker: {speaker_label} ({line_count})", then each line of `body.split("\n")` as a `<p>` (hono/jsx escapes text; do not use dangerouslySetInnerHTML). Link back to /sources.

    Do not log request bodies or transcript text anywhere.

    In `src/layout.tsx` add a `<nav>` in the header with links Home `/`, Sources `/sources`, Fireflies `/fireflies`, Settings `/settings`, Health `/health` (small inline CSS: inline list, gap 1rem). In `src/index.tsx` mount `app.route("/", sources)`.
  </action>
  <verify>
    With `npm run dev`: `curl -s localhost:8787/sources` → 200 containing "Voice samples" and the empty-state text. `curl -s -i -X POST -H "Origin: http://localhost:8787" --data-urlencode "body=Test post about Jersey fintech." localhost:8787/sources/samples` → 303; the next GET /sources contains "Test post about Jersey fintech.". Empty body POST → 400. `curl -s -o /dev/null -w "%{http_code}" localhost:8787/sources/transcripts/nope` → 404; `.../transcripts/bad%20id!` → 400. `npm run check` exits 0.
  </verify>
  <done>Samples can be pasted and listed; transcript list and detail render from D1; nav present on every page.</done>
</task>

</tasks>

<verification>
- `npm run check` exits 0
- Local and remote D1 have migration 0002 applied
- Settings round-trip and sample paste verified with curl as above
- `git grep -n "console.log" src/` returns nothing
</verification>

<success_criteria>
- All tasks completed, one commit per task
- VOICE-05 and VOICE-06 satisfied for samples; VOICE-06 transcript list/detail ready for 02-03's rows
- Speaker name persisted under `speaker.name` for 02-03 to read
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-sources/02-02-SUMMARY.md`
</output>
