---
phase: 06-accounts
plan: 05
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified: [migrations/0009_account_credentials.sql, src/db.ts, src/settings.tsx, src/fireflies-routes.tsx, src/zernio-routes.tsx, src/runs.tsx]
autonomous: true

user_setup:
  - service: cloudflare-workers
    why: "The credential master key is a Worker secret; only the operator can set it."
    env_vars:
      - name: CREDENTIAL_KEY
        source: "Generate 32 random bytes, base64 encoded. Claude can produce the value and add it to .dev.vars; pushing it to the Worker is `npm run secrets:push`, which the operator runs."

must_haves:
  truths:
    - "Each account saves its own Fireflies and Zernio credentials"
    - "One account's import or push can never use another account's credentials"
    - "A stored credential is unreadable in the database"
    - "An account with no credentials is told to add them, not shown someone else's connection"
  artifacts:
    - path: "migrations/0009_account_credentials.sql"
      provides: "per-account sealed credential storage"
      contains: "CREATE TABLE"
    - path: "src/settings.tsx"
      provides: "the screen where an account enters its own Fireflies and Zernio keys"
  key_links:
    - from: "src/fireflies-routes.tsx"
      to: "src/credentials.ts openCredential"
      via: "the caller's own sealed key, opened at the moment of use"
      pattern: "openCredential"
    - from: "src/runs.tsx push route"
      to: "the caller's Zernio credential"
      via: "per-account lookup replacing c.env.ZERNIO_USER_TOKEN"
      pattern: "openCredential"
---

<objective>
Move Fireflies and Zernio from one shared Worker secret to a credential each account owns.

Purpose: ACCT-05. Another user's meetings live in their Fireflies and their posts belong in
their Zernio — a shared key would import the operator's meetings for everyone and push every
account's drafts into one Zernio. This is the plan that reverses half of PLAT-03, on purpose.

Output: migration 0009, a credentials screen per account, and both integrations reading the
caller's own key.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accounts/DISCOVERY.md

@.planning/phases/06-accounts/06-03-SUMMARY.md
@.planning/phases/06-accounts/06-04-SUMMARY.md

@src/credentials.ts
@src/fireflies.ts
@src/zernio.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0009 and the sealed-credential helpers</name>
  <files>migrations/0009_account_credentials.sql, src/db.ts</files>
  <action>
    Create `account_credentials`: `owner_email`, `service` ('fireflies' | 'zernio'), the sealed
    record columns from `sealCredential`, `key_version`, `updated_at`. Primary key
    (owner_email, service) — one credential per service per account, and saving again replaces.

    Apply to local AND remote (retry once past the 7403 transient).

    Add owner-scoped helpers to `src/db.ts`: read one account's sealed credential for a service,
    upsert it, delete it. Owner is required, as everywhere else after 06-04. `src/db.ts` stores
    and returns the sealed record as opaque text and must NOT import `src/credentials.ts` — the
    data layer has no business holding the master key, and keeping it out is what stops a
    plaintext key reaching a log line or an error page.

    The migration header must state what is stored and what protects it, including the limit
    from DISCOVERY.md: sealed against a database dump, not against someone who can deploy the
    Worker.
  </action>
  <verify>
    Migration listed as applied on both databases. `grep -c '^import' src/db.ts` still 0.
    Insert a sealed row and confirm `SELECT` shows no plaintext.
  </verify>
  <done>Each account can have one sealed credential per service, stored and read by owner.</done>
</task>

<task type="auto">
  <name>Task 2: The credentials screen</name>
  <files>src/settings.tsx</files>
  <action>
    Add Fireflies and Zernio credential fields to the settings screen, in the Broadsheet style
    the rest of the app now uses (`src/theme.ts` tokens, `.pf-card`, `.btn`).

    Rules that are not negotiable:
      - The stored key is NEVER rendered back, not even masked from the real value. Show only
        whether a credential is set, and when it was last updated — the same discipline
        `/health` already follows for Worker secrets.
      - Saving an empty value deletes the credential rather than storing an empty string.
      - The POST validates shape before storing: Zernio keys are `sk_` + 64 hex (src/zernio.ts
        already knows this), Fireflies keys are opaque but length-capped.
      - A validation failure must not echo the submitted value back into the HTML.
  </action>
  <verify>
    Save a credential, reload, and confirm the page says "set" and never shows the value.
    `curl` the page and grep for the key text — it must not appear. Save empty → deleted.
  </verify>
  <done>An account can add, replace and remove its own keys without ever seeing them again.</done>
</task>

<task type="auto">
  <name>Task 3: Both integrations read the caller's own key</name>
  <files>src/fireflies-routes.tsx, src/zernio-routes.tsx, src/runs.tsx</files>
  <action>
    Replace `c.env.FIREFLIES_API_KEY` and `c.env.ZERNIO_USER_TOKEN` at every route with a
    lookup of the caller's own sealed credential, opened with `openCredential` at the moment of
    use and never held longer.

    `src/fireflies.ts` and `src/zernio.ts` already take the key as an argument and never read
    env — that boundary is why this task is small. Do not change it.

    No credential → the existing "not configured" page, pointing at /settings instead of at
    /health. The distinction matters: a missing Worker secret was the operator's problem, a
    missing account credential is the user's, and the page must say so.

    OpenAI stays `c.env.OPENAI_API_KEY`. The operator pays for drafting; that key is correctly
    one shared Worker secret and moving it would be wrong.

    Leave the FIREFLIES_API_KEY and ZERNIO_USER_TOKEN entries in `src/env.ts` SECRET_NAMES for
    now — removing them is a deploy-time concern and 06-06 owns the deploy. Note in the SUMMARY
    that they become dead once every account has its own.
  </action>
  <verify>
    Seed two accounts with two different (invalid-but-well-formed) Zernio keys and confirm each
    account's /zernio page fails with its OWN key's error, proving no sharing. Confirm an
    account with no credential gets the not-configured page and makes ZERO outbound requests
    (wrangler's local observability span count before and after, as 05-03 measured it).
  </verify>
  <done>Two accounts use two different keys; neither can reach the other's integration.</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` exits 0, `npm test` passes
- [ ] Migration 0009 on local AND remote
- [ ] No stored key is ever rendered
- [ ] Two seeded accounts demonstrably use different credentials
- [ ] No-credential path makes zero outbound requests, measured
- [ ] Local D1 returned to zero rows
</verification>

<success_criteria>
- Fireflies and Zernio are per-account and sealed at rest
- OpenAI deliberately unchanged, with the reason recorded
- The data layer still imports nothing
</success_criteria>

<output>
After completion, create `.planning/phases/06-accounts/06-05-SUMMARY.md`
</output>
