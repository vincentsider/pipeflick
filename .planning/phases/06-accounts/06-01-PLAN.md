---
phase: 06-accounts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [migrations/0008_accounts.sql]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Every row of user data carries the account that owns it"
    - "Every row that existed before this migration belongs to vincent@getinference.com"
    - "The health check's own settings row is not owned by any user and still works"
    - "Nothing is lost: the row counts before and after are identical"
  artifacts:
    - path: "migrations/0008_accounts.sql"
      provides: "owner_email on settings, transcripts, voice_samples, runs; backfill; indexes"
      contains: "ALTER TABLE transcripts ADD COLUMN owner_email"
      min_lines: 40
  key_links:
    - from: "migrations/0008_accounts.sql"
      to: "settings"
      via: "backfill that reserves the health key to the system owner"
      pattern: "UPDATE settings"
---

<objective>
Migration 0008: give every row of user data an owner, and give Vincent's existing data to him.

Purpose: ACCT-02 and ACCT-03 are columns before they are code. Landing the schema and the
backfill on their own — with nothing reading them yet — means the migration can be proved
correct against real row counts before any query depends on it, which is the order 04-01 and
05-02 used.

Output: `migrations/0008_accounts.sql`, applied to local and remote D1.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accounts/DISCOVERY.md

# The ownership shape and the reserved-system-owner problem are decided in DISCOVERY.md.
@migrations/0007_pilot_requests.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0008</name>
  <files>migrations/0008_accounts.sql</files>
  <action>
    Add `owner_email TEXT` to exactly four tables: `settings`, `transcripts`, `voice_samples`,
    `runs`. One ADD COLUMN per ALTER TABLE (SQLite takes no more). None may be NOT NULL —
    rows already exist in both databases.

    DO NOT add owner_email to `outliers` or `drafts`. They are reached only through their run
    (`recordDecision` is `WHERE id = ?4 AND run_id = ?5`, measured at changes() = 0 cross-run
    in 04-01), so scoping the run scopes them. Two sources of truth for one fact drift, and a
    draft whose owner disagrees with its run's owner is unrecoverable. Write this reasoning
    into the migration header — the next reader will otherwise "fix" the omission.

    DO NOT add owner_email to `pilot_requests`. Those are landing-page leads belonging to the
    operator, not to any user.

    Backfill: every existing row to 'vincent@getinference.com'.

    THE HEALTH KEY. `src/health.tsx` writes a settings row on every health check (the D1
    round-trip). That row is infrastructure, not user data, and must not belong to a user or
    the health check starts failing the moment ownership is enforced. Reserve it: backfill the
    health key's row to the system owner `''` (empty string), and say in the header that the
    empty string is the reserved system owner and is never a valid user identity — an Access
    identity is always a real address.

    Index `owner_email` on all four tables; every read after 06-04 filters on it.

    Header must also record the Jersey DP consequence: from this migration on, rows can belong
    to someone other than the operator, which is the point at which the project becomes a data
    processor for third parties.
  </action>
  <verify>
    Read the migration back and confirm: four ALTER TABLE ADD COLUMN statements, no NOT NULL,
    no owner_email on outliers/drafts/pilot_requests, four CREATE INDEX, and the health key
    reserved.
  </verify>
  <done>Migration file exists, is self-explaining, and touches exactly the four root tables.</done>
</task>

<task type="auto">
  <name>Task 2: Apply to local and remote, and prove nothing was lost</name>
  <files>migrations/0008_accounts.sql</files>
  <action>
    Capture row counts for all seven tables BEFORE applying (both local and remote), then run
    `npm run db:migrate:local` and `npm run db:migrate:remote`.

    Remote has failed with Cloudflare API error 7403 on 2 of 5 remote migrations and succeeded
    on an immediate retry — always retry once before re-authenticating.

    After applying, prove the three claims rather than assuming them:
      1. Row counts per table are identical before and after.
      2. `SELECT COUNT(*) FROM <t> WHERE owner_email IS NULL` is 0 on all four tables.
      3. `SELECT COUNT(*) FROM <t> WHERE owner_email != 'vincent@getinference.com'` is 0 on
         transcripts, voice_samples and runs, and on settings is exactly the health row.
    Record the actual numbers in the SUMMARY. "Applied successfully" is not evidence that the
    backfill was right.
  </action>
  <verify>
    `npx wrangler d1 execute pipeflick-db --remote --command "SELECT COUNT(*) ..."` for each
    claim above, local and remote, with the numbers pasted into the SUMMARY.
  </verify>
  <done>Both databases carry the columns, every pre-existing row is owned, counts unchanged.</done>
</task>

</tasks>

<verification>
- [ ] `npm run check` exits 0 (no code changed, but the migration must not break type generation)
- [ ] `npm test` passes unchanged
- [ ] Local and remote both list 0008 as applied
- [ ] Row counts identical before and after, on both databases
- [ ] No NULL owner_email on any of the four tables
</verification>

<success_criteria>
- Migration applied to local AND remote
- Every pre-existing row belongs to vincent@getinference.com, except the reserved health row
- outliers, drafts and pilot_requests deliberately untouched, with the reason written down
- Nothing deployed — no code reads these columns yet
</success_criteria>

<output>
After completion, create `.planning/phases/06-accounts/06-01-SUMMARY.md`
</output>
