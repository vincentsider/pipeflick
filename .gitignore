# --- Secrets: never commit. Values live in .dev.vars locally and Worker secrets remotely. ---
.env
.env.*
!.env.example
.mcp.json
.dev.vars*
!.dev.vars.example
# Generic credential shapes, so a stray file cannot be added by accident
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
secrets.json
*.tfvars

# --- Build, deps, generated ---
node_modules/
.wrangler/
dist/
# Generated by `wrangler types`; regenerate with `npm run types`
worker-configuration.d.ts

# --- Local noise, not part of this project ---
# logs/ is output from an unrelated Pega MCP plugin; cache/ is likewise not ours
logs/
*.log
cache/
.DS_Store
.claude/settings.local.json

# Pilot PRD: business strategy, kept out of the public repo
prd.md

# Local Worker config: holds the real D1 database id and dev identity.
# Copy wrangler.example.jsonc -> wrangler.jsonc and fill in your own values.
wrangler.jsonc
